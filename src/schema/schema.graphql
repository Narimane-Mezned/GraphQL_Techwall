enum Status { 
  TODO
  IN_PROGRESS
  DONE
}
// enum c pas Enum !!!!


type Todo {
  id: ID!
  title: String!
  content: String!
  status: Status!
}
type Query {
  hello(name: String!): String
  getTodos: [Todo]!
}
